# 文件页人物与页面切换动效改动计划

## 1. 目标

为首页、任务页、文件页建立统一且可逆的页面动效系统，使同一个人物在不同页面承担明确语义：

- 首页：感知待命态。
- 任务页：聚合执行态。
- 文件页：记忆检索态。

重点完成首页进入文件页、任务页进入文件页，以及文件页返回其他页面时的人物状态变化、内容交接、导航反馈和输入条变形。不得更换或交叉淡入第二个人物，不增加无意义的粒子、全屏扫光或持续装饰动画。

## 2. 本次使用的 Skill

| Skill | 使用阶段 | 具体作用 |
| --- | --- | --- |
| `product-design:index` | 工作流路由 | 确认本次属于已有视觉源上的产品动效扩展，不重新设计页面结构。 |
| `product-design:get-context` | 动效设计前 | 复核产品、目标页面、用户目的和既有视觉源，避免偏离已选中的首页、任务页和文件页设计。 |
| `product-design:image-to-code` | 素材与实现 | 将 `file-memory-chamber-v1.png` 中的文件态人物表现还原到现有共享人物结构中。 |
| `product-design:design-qa` | 每阶段验收 | 将设计源和真实浏览器截图放在同一比较输入中，检查人物位置、状态、节奏、轨道关系和页面层级。 |
| `imagegen` | 必要时生成素材 | 只在现有素材无法表达文件态时，生成一张与人物严格对齐的透明记忆神经场，不生成第二个人物。 |
| `hyperframes:gsap` | 时间轴规划 | 定义 GSAP 标签、时间点、缓动、交错、打断、反向播放和清理方式。 |
| `gsap-react` | React 实现 | 使用 `useGSAP`、作用域 Ref、`contextSafe` 和卸载清理，避免残留 Tween。 |
| `gsap-performance` | 性能控制 | 优先使用 transform 和 opacity，限制滤镜面积，避免布局抖动和无效 `will-change`。 |
| `design-taste-frontend` | 审美约束 | 控制动效密度，避免模板化淡入、无限循环、彩虹光效和每个元素都动。 |
| `impeccable` 的 `animate` 与 `product` 规范 | 产品动效质量 | 确保每个动画表达状态、反馈或层级，控制时长，并完整支持减少动态效果。 |
| `browser:control-in-app-browser` | 实机验收 | 在 Codex 内置浏览器中检查真实页面、切换中间态、快速反向操作、截图和控制台错误。 |

设计参数固定为：`DESIGN_VARIANCE 6 / MOTION_INTENSITY 7 / VISUAL_DENSITY 5`。

## 3. 当前问题

1. 首页进入文件页仍借用首页到任务页的主时间轴，人物先进入执行语义，再显示文件内容，语义不准确。
2. 任务页和文件页之间主要是面板平移与透明度交接，人物没有从执行态切换成记忆检索态。
3. 文件页人物缺少独立的头部索引、核心连接和资料轨道展开过程。
4. 当前路由动效、人物动效和内容面板动效耦合较多，继续增加成对时间轴后容易出现抢占、闪帧和反向不连续。
5. 文件导航没有与任务导航同等级的悬停和键盘聚焦预感知反馈。

## 4. 总体技术结构

将现有逻辑整理为三层，不再为每一对页面单独堆一套动画。

### 4.1 `routeTimeline`

负责：

- 首页与工作区的整体空间切换。
- 共享人物的位置、缩放和整体透明度。
- 导航玻璃块移动。
- 持久输入条的 Flip 几何变化。

### 4.2 `companionStateTimeline`

负责 `home / tasks / files` 三种人物状态：

- `home`：现有透明人物、状态节点和待命核心。
- `tasks`：现有 visor、core、neural 执行区域。
- `files`：新增 memory visor、memory core、memory links 文件记忆层。

要求：

- 同一个 `.companion-shared-figure` 始终挂载。
- 页面快速切换时从当前进度转向新目标，不先归零。
- 人物状态层只动画 transform 和 opacity。
- 任务执行光束、文件选择信号等瞬时事件不放进路由主时间轴。

### 4.3 `panelTimeline`

负责：

- 任务内容和文件内容的短促交接。
- 文件轨道、预览区、搜索、上传入口的分组入场。
- 面板隐藏态在时间轴开始前通过 `gsap.set` 建立，避免首次切换闪烁。

## 5. 分阶段改动

### 阶段一：首页进入文件页

目标：建立文件页的主签名动效，先解决最重要的页面语义。

时间预算：约 `680ms`。

| 时间 | 动作 |
| --- | --- |
| `0-100ms` | 文件导航按压反馈，导航玻璃块开始滑动。 |
| `60-220ms` | 首页标题、能力区和状态节点向左收束退出。 |
| `100-360ms` | 人物向右移动并轻微缩小，视觉中心转向资料空间。 |
| `140-400ms` | 文件记忆层从头部向胸口建立一次有限连接。 |
| `180-500ms` | 四条资料轨道从人物核心向左展开，第一条略早，其余短促交错。 |
| `260-560ms` | 默认选中文件发出一次信号，右侧预览随后落位。 |
| `300-680ms` | 输入条通过 Flip 进入紧凑形态，搜索和上传入口最后就绪。 |

涉及文件：

- `src/App.jsx`
- `src/pageMotion.js`
- `src/FilesWorkspace.jsx`
- `src/styles.css`
- 可能新增 `public/assets/companion-memory-map-v1.webp`

验收：

- 人物没有先进入任务执行态。
- 文件轨道像由人物展开，而不是普通列表淡入。
- 连续点击任务、文件、首页时没有闪帧、重影或输入条尺寸异常。
- 减少动态效果模式可直接进入稳定文件状态。

### 阶段二：任务页与文件页双向切换

目标：让人物在“执行任务”和“调取记忆”之间快速转态。

时间预算：`360-420ms`。

任务页到文件页：

1. 当前执行光束停止，活跃能量收回核心。
2. 任务内容向左退出 `12-14px`。
3. 执行区域收束，文件记忆层展开。
4. 文件轨道与预览从人物方向进入。
5. 输入条保持紧凑几何，只更新内容和工具状态。

文件页到任务页：

1. 当前文件信号完成后停止，不机械倒放点击反馈。
2. 四条轨道由远到近收回核心。
3. 文件记忆层关闭，执行区域恢复。
4. 任务内容从保留的阅读位置恢复，不重播完整首页入场。

涉及文件：

- `src/App.jsx`
- `src/pageMotion.js`
- `src/styles.css`

验收：

- 两个工作面板切换不经过首页状态。
- 人物语义状态和内容面板始终一致。
- 切换中途反向操作能从当前进度自然返回。
- 任务执行阶段切换到文件页后，执行光束和完成波纹不会残留。

### 阶段三：文件页返回首页

目标：建立与首页进入文件页对应但不机械倒放的返回动作。

时间预算：`580-680ms`。

动作：

1. 资料轨道回收到人物核心。
2. 文件记忆层收束，人物恢复首页位置和尺寸。
3. 首页状态节点重新连接人物对应区域。
4. 标题和能力区在人物接近落位后出现。
5. 输入条通过 Flip 恢复首页大输入形态。

验收：

- 文件点击、上传或拖拽等瞬时反馈不会被倒放。
- 首页状态卡与人物不会发生遮挡。
- 返回完成后人物呼吸、视差和首页交互继续正常工作。

### 阶段四：文件导航预感知

目标：鼠标悬停或键盘聚焦“文件”时，人物提前表达记忆意图。

动作：

- 头部出现一圈低透明度记忆折射。
- 胸口核心向文件导航方向轻触一次。
- 不移动人物，不展开资料轨道。
- 离开后约 `220ms` 恢复。
- 点击时由预览状态直接交给正式文件时间轴。

实现建议：

- 将现有任务预览扩展为 `previewMode: tasks | files | null`。
- 任务预览和文件预览共用 API，但使用不同区域和时间轴标签。

### 阶段五：文件页内部反馈统一

范围：

- 文件搜索获得焦点时，只增强对应轨道局部亮度。
- 文件选中继续使用按压、涟漪、轨道信号、预览落位和人物核心回应。
- 上传资料时，信号进入人物核心后落到目标轨道，并显示索引状态。
- 外部文件拖入资料库时显示接收反馈。
- 已索引资料拖入输入条时只表达引用，不表达重新入库。
- 搜索无结果时，轨道降低活跃度，人物记忆环收成一圈。
- 所有效果都是有限事件，不增加常驻粒子和轨道循环旋转。

涉及文件：

- `src/FilesWorkspace.jsx`
- `src/App.jsx`
- `src/styles.css`

## 6. GSAP 实现约束

- 使用带标签的 `gsap.timeline()` 管理节拍，不用多个 `delay` 串联。
- 高频指针反馈使用 `gsap.quickTo()`。
- React 内统一使用 `useGSAP`，事件回调使用 `contextSafe`。
- 页面切换前 `kill` 或接管相同目标的旧 Tween，不允许并发争抢同一 transform。
- 人物外层 transform 归路由时间轴所有，人物内部语义层 transform 归状态时间轴所有。
- 输入条外层 transform 继续只归 Flip 所有，焦点反馈留在内部折射层。
- 不动画 `top / left / width / height` 完成移动。
- `will-change` 只在真实动画期间启用，完成后清除。
- 退出时长约为进入时长的 `75%`。

## 7. 减少动态效果

在 `prefers-reduced-motion: reduce` 下：

- 不播放人物位移、轨道展开、信号飞行和分组交错。
- 直接设置目标页面、人物语义层和输入条的最终状态。
- 保留导航选中、文件选中、上传进度和错误状态等必要反馈。
- 页面内容不得依赖动画完成后才变得可见。

## 8. 验证计划

每个阶段完成后只验证本阶段相关路径：

1. 在 Codex 内置浏览器以同一视口录入首页、任务页、文件页稳定状态截图。
2. 验证首页到文件、任务到文件、文件到任务、文件到首页。
3. 每条路径测试一次中途反向切换。
4. 验证快速连续点击不会产生人物重影、导航错位或输入条残留 transform。
5. 验证普通模式和减少动态效果模式。
6. 检查浏览器 warning 和 error。
7. 运行 `npm run build`。
8. 运行 `npm run test:sites`。
9. 更新 `design-qa.md`，保留设计源、真实截图、比较历史和 `final result`。

## 9. 实施顺序与停线点

实施顺序：

1. 首页进入文件页。
2. 任务页与文件页双向切换。
3. 文件页返回首页。
4. 文件导航预感知。
5. 文件页内部反馈统一。
6. 浏览器视觉验收、构建和 Sites 测试。

每完成一个阶段先取得真实浏览器证据再进入下一阶段。若共享人物发生重影、输入条 Flip 异常、首次内容闪烁或快速反向失效，应停止追加效果，先修复状态所有权和时间轴冲突。

## 10. 非本次范围

- 不修改页面信息架构、路由名称和导航文案。
- 不重做首页、任务页或文件页布局。
- 不替换正式品牌资产。
- 不新增第二个人物或全身过渡序列。
- 不部署、不发布、不提交或推送代码。

